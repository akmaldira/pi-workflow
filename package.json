{
  "name": "pi-workflow",
  "version": "0.1.0",
  "description": "Pi extension for delegating tasks to specialized subagents",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "dependencies": {
    "acorn": "^8.16.0"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
